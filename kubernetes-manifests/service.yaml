apiVersion: v1
kind: Service
metadata:
  name: my-node-app-service
spec:
  selector:
    app: my-node-app # This MUST match the label in your Deployment
  ports:
    - protocol: TCP
      port: 80 # The port the Service will listen on
      targetPort: 3001 # The port to forward traffic to on the container